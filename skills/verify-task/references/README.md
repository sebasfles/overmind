# Stack fallbacks

Used only when `docs/TRD.md` does not declare verification commands.
One file per stack with lint, typecheck, unit and e2e commands and their serial flags.

Planned: `nestjs.md`, `nextjs.md`, `rails.md`, `react-native.md`.
